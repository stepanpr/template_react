{
    "compilerOptions": {
		"outDir": "./dist/",
        "noImplicitAny": true,
        "target": "es5",
        "jsx": "react",
		"allowJs": true,
		"sourceMap": true,
        "noEmitOnError": true,
        "noUnusedLocals": false, //поменять на true
        "removeComments": true,
        "esModuleInterop": true,
        "baseUrl": "./src",
		// "paths": { // Указываем ссылки до директорий в проекте, чтобы каждый раз в импортах не писать относительные пути
		// // "app": ["src/app"],
		// // "pages/*": ["src/pages/*"],
		// // "widgets/*": ["src/widgets/*"],
		// }
    },
    "exclude": [
        "node_modules"
    ]
}